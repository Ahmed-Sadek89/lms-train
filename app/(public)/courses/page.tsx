import PublicCoursesSelectionWrapper from '@/components/ui/public-courses-selection-wrapper'

const Courses = () => {
    return (
        <PublicCoursesSelectionWrapper
        // courses={[]}
        // categories={[]}
        // tools={[]}
        />
    )
}

export default Courses